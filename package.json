{
  "name": "atomic-round-trip",
  "version": "1.0.0",
  "description": "Native Solana atomic round-trip swaps on Raydium",
  "main": "client.ts",
  "scripts": {
    "build": "cargo build-bpf --manifest-path=Cargo.toml --bpf-out-dir=target/deploy",
    "deploy": "solana program deploy target/deploy/atomic_swap.so",
    "test": "ts-node client.ts"
  },
  "dependencies": {
    "@solana/web3.js": "^1.87.6",
    "@solana/spl-token": "^0.3.9",
    "borsh": "^0.7.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.1"
  }
}
